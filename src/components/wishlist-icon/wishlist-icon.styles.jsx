import styled from "styled-components";

export const WishlistIconContainer = styled.div`
  width: 18px;
  height: 18px;
  margin-left: 10px;
  display: block;
  border: none;

  &:hover {
    cursor: pointer;
  }
`;
